int[] a = { 1, 2, 3, 4, 5, 6, 7, 8 };

for (int i = 0; i < a.Length; i++)
{
    Console.WriteLine(a[i]);
}

int[] b = [1, 2, 3, 4, 5, 6, 7, 8];

for (int i = 0; i < a.Length; i++)
{
    Console.WriteLine(a[i]);
}



//List<int> с = { 1, 2, 3, 4, 5, 6, 7, 8 };

//for (int i = 0; i < a.Length; i++)
//{
//    Console.WriteLine(a[i]);
//}

List<int> d = [ 1, 2, 3, 4, 5, 6, 7, 8 ];

for (int i = 0; i < a.Length; i++)
{
    Console.WriteLine(a[i]);
}